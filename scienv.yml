name: scienv
channels:
    - conda-forge
    - anaconda
dependencies:
    # python is locked at 3.1 to be compatible with IMP's conda build
    - python
    - numpy
    - scipy
    - scikit-learn
    - matplotlib
    - pandas
    - biopython
    - tqdm
    - joblib
    - cmake
    - imp
